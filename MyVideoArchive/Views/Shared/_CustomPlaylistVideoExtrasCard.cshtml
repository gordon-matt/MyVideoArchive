@model bool
@{
    ViewData["VideoVisibleBinding"] = "currentVideo()";
    ViewData["AssociateEmptyHint"] = "Use \"Associate items…\" to pick from files on this video's channel.";
}
@await Html.PartialAsync("_VideoExtrasCard", Model)
