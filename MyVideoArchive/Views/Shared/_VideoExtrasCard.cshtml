@model bool
@{
    // channel-playlist | page-video | user-playlist
    var context = ViewData["ExtrasContext"] as string ?? "page-video";
    var videoVisible = context is "channel-playlist" or "user-playlist" ? "currentVideo()" : "video()";
    var associateHint = context == "channel-playlist"
        ? "Use \"Associate items…\" to pick from files available for this playlist."
        : "Use \"Associate items…\" to pick from files on this video's channel.";
    var showAssociate = Model;
}
<div class="card mt-3" data-bind="visible: @videoVisible">
    <div class="card-header d-flex flex-wrap justify-content-between align-items-center gap-2">
        <h6 class="mb-0"><i class="bi bi-paperclip me-1"></i>Extras for this video</h6>
        @if (showAssociate)
        {
            <button type="button" class="btn btn-sm btn-outline-primary"
                    data-bind="click: openExtrasPicker, disable: extrasPickerLoading">
                <i class="bi bi-plus-lg me-1"></i>Associate items&hellip;
            </button>
        }
    </div>
    <div class="card-body p-2">
        <div data-bind="visible: extrasLoading" class="text-center py-2">
            <div class="spinner-border spinner-border-sm" role="status"></div>
        </div>
        <div data-bind="visible: !extrasLoading() && extrasItems().length === 0" class="text-muted small p-2">
            No additional files linked to this video yet.
            @if (showAssociate)
            {
                <text> @associateHint</text>
            }
        </div>
        <div data-bind="visible: !extrasLoading() && extrasItems().length > 0">
            <div class="table-responsive">
                <table class="table table-sm table-hover align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>Item</th>
                            <th class="video-extras-actions">Actions</th>
                        </tr>
                    </thead>
                    <tbody data-bind="foreach: extrasItems">
                        <tr>
                            <td>
                                <i class="bi bi-file-earmark me-1 text-muted"></i>
                                <!-- ko if: $parent.isPreviewableExtra && $parent.isPreviewableExtra(fileName) -->
                                <a href="#" class="link-body-emphasis"
                                   data-bind="text: fileName, click: function(row) { $parent.openExtraPreview(row); return false; }"></a>
                                <!-- /ko -->
                                <!-- ko ifnot: $parent.isPreviewableExtra && $parent.isPreviewableExtra(fileName) -->
                                <a data-bind="attr: { href: '/api/additional-content/' + id + '/download' }, text: fileName"
                                   target="_blank" rel="noopener noreferrer"
                                   class="link-body-emphasis"></a>
                                <!-- /ko -->
                            </td>
                            <td class="video-extras-actions">
                                <a class="btn btn-sm btn-outline-secondary"
                                   data-bind="attr: { href: '/api/additional-content/' + id + '/download?isForDownload=true' }"
                                   target="_blank" title="Download">
                                    <i class="bi bi-download"></i>
                                </a>
                                @if (Model)
                                {
                                    <button class="btn btn-sm btn-outline-warning ms-1"
                                            data-bind="click: $parent.removeExtraFromVideo" title="Remove from this video">
                                        <i class="bi bi-link-45deg"></i>
                                    </button>
                                }
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
